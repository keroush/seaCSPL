# شرکت حمل و نقل دریایی - SeaCSPL

پروژه Frontend برای شرکت حمل و نقل دریایی با استفاده از Next.js 15، TypeScript و Tailwind CSS v4

## 🚀 تکنولوژی‌ها

- **Next.js 16** (App Router)
- **TypeScript**
- **Tailwind CSS v4**
- **Zustand** (State Management)
- **React 19**
- **Shadcn UI Components**
- **Leaflet** (برای نقشه - در صورت نیاز)

## 📁 ساختار پروژه

```
├── src/
│   ├── app/                    # مسیرهای Next.js (App Router)
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── globals.css
│   │   ├── auth/
│   │   ├── dashboard/
│   │   ├── tracking/
│   │   ├── services/
│   │   ├── about/
│   │   └── contact/
│   ├── components/
│   │   ├── layout/             # کامپوننت‌های Layout
│   │   │   ├── header/
│   │   │   ├── footer/
│   │   │   └── main-layout.tsx
│   │   ├── ui/                 # کامپوننت‌های UI قابل استفاده مجدد
│   │   │   ├── button.tsx
│   │   │   ├── input.tsx
│   │   │   ├── modal.tsx
│   │   │   └── card.tsx
│   │   └── modules/            # ماژول‌های صفحات
│   │       ├── home/
│   │       ├── auth/
│   │       └── dashboard/
│   ├── lib/                    # توابع کمکی
│   │   ├── fetcher.ts
│   │   ├── cookies.ts
│   │   ├── utils.ts
│   │   └── api-routes.ts
│   ├── stores/                 # Zustand stores
│   │   └── auth-store.ts
│   └── config/                 # تنظیمات
│       └── env.ts
└── public/                     # فایل‌های استاتیک
```

## 🏃 شروع کار

### پیش‌نیازها

- Node.js 18+ 
- npm یا yarn

### نصب وابستگی‌ها

```bash
npm install
```

### تنظیمات محیطی

فایل `.env.local` را ایجاد کنید:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### اجرای سرور توسعه

```bash
npm run dev
```

سپس مرورگر را باز کنید و به [http://localhost:3000](http://localhost:3000) بروید.

### ساخت پروژه برای Production

```bash
npm run build
npm start
```

## ✨ ویژگی‌ها

- 🏠 **صفحه اصلی**: نمایش خدمات و آمار شرکت
- 🔐 **احراز هویت**: ورود/ثبت نام با OTP
- 📦 **ردیابی محموله**: ردیابی لحظه‌ای محموله‌ها
- 🚢 **خدمات**: نمایش خدمات مختلف حمل و نقل دریایی
- 👤 **پنل کاربری**: مدیریت سفارش‌ها و اطلاعات کاربری
- 📞 **تماس با ما**: فرم تماس و اطلاعات تماس
- ℹ️ **درباره ما**: اطلاعات شرکت و تیم

## 📝 قوانین پروژه

این پروژه طبق قوانین تعریف شده در `meta_prompt.txt` توسعه یافته است:

- ✅ معماری feature-based و ماژولار
- ✅ استفاده از TypeScript و Tailwind CSS v4
- ✅ SSR-first با استفاده محدود از CSR
- ✅ مدیریت state با Zustand
- ✅ احراز هویت با OTP و ذخیره در Cookie
- ✅ رعایت اصول SOLID و Clean Code
- ✅ کامپوننت‌های قابل استفاده مجدد
- ✅ طراحی Responsive و Accessible

## 🔗 اتصال به Backend

این پروژه برای اتصال به Laravel Backend طراحی شده است. 
مسیرهای API در `src/lib/api-routes.ts` تعریف شده‌اند.

## 📄 لایسنس

این پروژه خصوصی است.
